# Flows

Flow pages 描述跨本機工具、identity、Azure control plane、data plane、workload 與 observability systems 的可重現過程。

## Registry

目前尚無已定義 flow。

| Flow | Scope | Status |
| --- | --- | --- |
| None | — | — |

## Flow rules

每個 flow 要列出 execution location、trigger、preconditions、actor/identity、tenant/subscription/resource scope、state before/after、side effects、cost/security impact、failure classification、recovery、cleanup 與 evidence。

